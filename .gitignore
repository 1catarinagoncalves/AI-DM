node_modules/
.env
.env.local
dist/
.next/
build/
*.tsbuildinfo
.turbo/

# US-47: dataset SRD cru baixado pelo sync (só o artefato srd-5e.config.json entra no repo)
scripts/srd/_data/

# Prisma 7: client gerado pelo `prisma generate` (output obrigatório no generator)
apps/api/src/generated/

.vercel
.env*

# US-36: relatórios de eval gerados por execução (narração + notas + metadados)
evals/reports/
