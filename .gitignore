node_modules/
.env
.env.local
dist/
.next/
build/
*.tsbuildinfo
.turbo/

# US-47: dataset SRD cru baixado pelo sync (só os artefatos srd-5e.config.<locale>.json entram no repo)
scripts/srd/_data/

# US-145: dataset LGMRD cru baixado pelo sync (só NOTICE-lazygm.md entra no repo)
scripts/lazygm/_data/

# Prisma 7: client gerado pelo `prisma generate` (output obrigatório no generator)
apps/api/src/generated/

.vercel
.env*

# US-36: relatórios de eval gerados por execução (narração + notas + metadados)
evals/reports/

# US-78: estado de UI do vault Obsidian (churn a cada scroll) e plugins de terceiros
docs/.obsidian/workspace.json
docs/.obsidian/appearance.json
docs/.obsidian/graph.json
docs/.obsidian/plugins/

# Cache do plugin impeccable (uma pasta por diretório visitado, 10 e contando)
.impeccable/

# graphify: só graph.json e o relatório são versionáveis. O resto é local da
# máquina (.graphify_python guarda caminho absoluto do venv) ou artefato de build.
graphify-out/*
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md
